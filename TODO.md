# TODO: Fix Import Errors in Backend

- [x] Add __init__.py to backend/ to make it a Python package
- [x] Change imports in app.py to relative (from .phishtank import check_phishtank, etc.)
- [x] Remove unused import in phishtank.py (import requests)
- [x] Remove unused import in ml_model.py (import joblib)
- [x] Test running the app with python -m backend.app from project root
